
# Jekyll Portfolio Template

Fully functional Jekyll portfolio with responsive hero, navigation, multiple pages, and GitHub Actions deployment.

## Setup

1. Clone repository
2. Install Ruby >= 3.0
3. Install Bundler: `gem install bundler`
4. Install dependencies: `bundle install`
5. Run locally: `bundle exec jekyll serve`
6. Push to main branch to deploy automatically
